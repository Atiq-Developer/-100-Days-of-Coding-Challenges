// Attempting to use a reserved word as a variable name

// let if = 5; // This line would cause a syntax error if I Uncomment it

console.log(
  "Using a reserved word as a variable name causes a syntax error in JavaScript."
);
