// Making a simple List of Number
let List1 = [1, 3, 5, 7, 9];
/*Now applying a map method.map is an array method that creates a new array
by applying a provided function to each element of the original array.*/
let doubleListof_List1 = List1.map((number) => number * 2);
console.log(doubleListof_List1);
export {};
