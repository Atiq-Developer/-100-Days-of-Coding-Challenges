//  Making a function which return modulus
function modulus(num1: number, num2: number): number {
  return num1 % num2;
}
let remainder = modulus;
console.log(`The remainder is : ${remainder(16, 3)}`);
